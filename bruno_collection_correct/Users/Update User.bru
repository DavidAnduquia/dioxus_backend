meta {
  name: Update User
  type: http
  seq: 5
}

put {
  url: {{BASE_URL}}/api/usuarios/{{USER_ID}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

body: json {
  {
    "email": "actualizado@example.com",
    "name": "Usuario Actualizado",
    "role_id": "{{ROLE_ID}}"
  }
}
